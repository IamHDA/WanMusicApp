spring:
  application:
    name: WanMusic

  datasource:
#    url: jdbc:mysql://localhost:3306/wanmusic?useSSL=false&serverTimezone=UTC
#    username: root
#    password: 3355
    url: jdbc:mysql://100.97.109.94:3306/wanmusic?useSSL=false&serverTimezone=UTC
    username: mobile
    password: Mobile123@

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 7000

jwt:
  secret-string: ${JWT_SECRET}

storage:
  endpoint: ${STORAGE_ENDPOINT}
  access-key: ${STORAGE_ACCESS_KEY}
  secret-key: ${STORAGE_SECRET_KEY}

payos:
  client-id: ${PAYOS_CLIENT_ID}
  api-key: ${PAYOS_API_KEY}
  checksum-key: ${PAYOS_CHECKSUM_KEY}

app:
  frontend:
    url: "http://localhost:8081/payments/success"
    cancel-url: "http://localhost:8081/payments/cancel"